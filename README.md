# dbml-tools

The project should provide

- A database visualiser (via open source dot files)
- A database documentation builder (with a open source hugo output)

Future work:

- A tool to create a DBML file from a database connection
